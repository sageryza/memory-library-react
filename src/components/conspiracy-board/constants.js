// Canvas dimensions and offsets
export const CANVAS_OFFSET_X = 4500
export const CANVAS_OFFSET_Y = 3000

// Memory card dimensions (visual card size from CSS)
export const CARD_WIDTH = 200
export const CARD_HEIGHT = 120
export const CARD_WIDTH_STACKED = 120

// Pin position offsets (relative to card)
export const PIN_OFFSET_FROM_RIGHT = 8  // Distance from right edge to center of pin tail
export const PIN_OFFSET_FROM_TOP = 7    // Bottom of pin tail (-17px + 24px)

// Standalone pin dimensions
export const STANDALONE_PIN_CENTER_X = 10  // Pin is 20px wide, center at 10
export const STANDALONE_PIN_BOTTOM_Y = 24  // Top: 10px + tail height: 14px
